Namespace('Reaper'); // base namespace
Namespace('ReaperIface'); // base interface

Reaper = Class.extend({
	init: function(params) {
		this._ = $.extend(true, {
			
		});

		return this._;
	},

	bootstrap: function() {
	}
});

Class.addSingleton(Reaper);